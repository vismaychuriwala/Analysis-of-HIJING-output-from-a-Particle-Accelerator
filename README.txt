# final_hep_vismay
This repository contains code meant for 
Copy all the files in the same Directory and run . TheFinalTouch.sh ${data_file} ${Directory for unpacked files(so that data_file is untouched)}
